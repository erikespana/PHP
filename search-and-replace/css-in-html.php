// create array of patterns to match, ie: "align-right"
$styles = ["align-right", "align-center"]
/*

foreach page in pages
  echo page
  foreach style in styles
    if (preg_match (style, page.wysiwyg)
      echo "page has style";
    else
      echo "0";
  }
 }
*/
